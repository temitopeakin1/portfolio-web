# Local dev only — loaded by dev-api-server.mjs. This file is gitignored and is NOT deployed.
#
# Netlify (https://temitopeakinmegha.netlify.app): Site configuration → Environment variables
# Add ADMIN_PASSWORD (and optional ADMIN_SECRET, OPENAI_API_KEY). Scope: Functions or All. Redeploy.
ADMIN_PASSWORD=your_secure_password
ADMIN_SECRET=optional_random_string
OPENAI_API_KEY=your_openai_key
